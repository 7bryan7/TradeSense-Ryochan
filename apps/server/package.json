{
  "name": "@tradesense/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "doctor": "npm run build --silent && node dist/cli.js doctor",
    "db:migrate": "npm run build --silent && node dist/cli.js migrate",
    "smoke:live": "npm run build --silent && node dist/cli.js smoke-live",
    "replay": "npm run build --silent && node dist/cli.js replay"
  },
  "dependencies": {
    "@tradesense/adapters": "*",
    "@tradesense/core": "*",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/cors": "latest",
    "@types/express": "latest",
    "tsx": "latest"
  }
}
